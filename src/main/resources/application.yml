server:
  address: ${SERVER_HOST:0.0.0.0}
  port: ${SERVER_PORT:8080}
  servlet:
    context-path: /bocompawAdmin

spring:
  application:
    name: qwenpaw-controller
  cloud:
    nacos:
      config:
        server-addr: 182.119.168.81:8848 # 配置中心地址
        file-extension: yaml
      discovery:
        server-addr: 182.119.168.81:8848 # 注册中心地址
    # JUMP 专属配置
    jump:
      error-code:
        prefix: EAGP # 例如：BITS
      log:
        enable: true
  jackson:
    property-naming-strategy: SNAKE_CASE
  data:
    redis:
      host: ${REDIS_HOST:12.244.71.125}
      port: ${REDIS_PORT:6379}
      password: ${REDIS_PASSWORD:EAGP@2026}
      timeout: 2000ms
      lettuce:
        pool:
          max-active: 50
          max-idle: 20
          min-idle: 5
  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 100MB
  profiles:
    active: uat
  main:
    allow-bean-definition-overriding: true


  datasource:
    # 默认库连接（不指定 profile 时的兜底值）
    url: ${DB_URL:jdbc:mysql://obproxy-1df7.ops.p04r1cloud.bocomm.com:3306/eagpdb?autoReconnection=true&useSSL=true&useUnicode=true&characterEncoding=UTF-8&allowMultiQueries=true&rewriteBatchedStatements=true&serverTimezone=Asia/Shanghai&connectionTimeZone=Asia/Shanghai&forceConnectionTimeZoneToSession=true}
    username: ${DB_USERNAME:eagpuser@teagpprd00#obp2cgb09prd}
    password: ${DB_PASSWORD:EagpUser_123#}
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: ${DB_POOL_SIZE:10}
      minimum-idle: ${DB_MIN_IDLE:2}
      connection-timeout: ${DB_CONN_TIMEOUT:30000}
      idle-timeout: ${DB_IDLE_TIMEOUT:600000}
      max-lifetime: ${DB_MAX_LIFETIME:1800000}

mybatis:
  mapper-locations: classpath:mapper/**/*.xml
  type-aliases-package: com.qwenpaw.controller.model.po
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.slf4j.Slf4jImpl

management:
  endpoints:
    web:
      base-path: /actuator
      exposure:
        include: health,info

logging:
  level:
    root: ${SERVER_LOG_LEVEL:info}

qwenpaw:
  k8s-namespace: ${K8S_NAMESPACE:ai}
  qwenpaw-app-label: ${QWENPAW_APP_LABEL:qwenpaw}
  qwenpaw-image: ${QWENPAW_IMAGE:docker.io/agentscope/qwenpaw:latest}
  qwenpaw-container-port: ${QWENPAW_CONTAINER_PORT:8088}
  qwenpaw-configmap-name: ${QWENPAW_CONFIGMAP_NAME:qwenpaw-global-config}
  qwenpaw-runtime-configmap-name: ${QWENPAW_RUNTIME_CONFIGMAP_NAME:qwenpaw-runtime-config}
  qwenpaw-nas-pvc-name: ${QWENPAW_NAS_PVC_NAME:qwenpaw-nas-pvc}
  qwenpaw-public-template-sub-path: ${QWENPAW_PUBLIC_TEMPLATE_SUB_PATH:public-secret}
  qwenpaw-volume-mode: ${QWENPAW_VOLUME_MODE:subpath}
  qwenpaw-nas-mount-path: ${QWENPAW_NAS_MOUNT_PATH:/qwenpaw_nas}
  personal-data-mount-path: ${PERSONAL_DATA_MOUNT_PATH:/qwenpaw_nas/personalData}
  resource-requests-cpu: ${RESOURCE_REQUESTS_CPU:500m}
  resource-requests-memory: ${RESOURCE_REQUESTS_MEMORY:1Gi}
  resource-limits-cpu: ${RESOURCE_LIMITS_CPU:1}
  resource-limits-memory: ${RESOURCE_LIMITS_MEMORY:2Gi}
  liveness-probe-initial-delay: ${LIVENESS_PROBE_INITIAL_DELAY:30}
  liveness-probe-period: ${LIVENESS_PROBE_PERIOD:10}
  liveness-probe-timeout: ${LIVENESS_PROBE_TIMEOUT:5}
  liveness-probe-failure-threshold: ${LIVENESS_PROBE_FAILURE_THRESHOLD:3}
  readiness-probe-initial-delay: ${READINESS_PROBE_INITIAL_DELAY:10}
  readiness-probe-period: ${READINESS_PROBE_PERIOD:5}
  readiness-probe-timeout: ${READINESS_PROBE_TIMEOUT:3}
  readiness-probe-failure-threshold: ${READINESS_PROBE_FAILURE_THRESHOLD:2}
  gateway-name: ${GATEWAY_NAME:traefik-gateway}
  gateway-namespace: ${GATEWAY_NAMESPACE:ai}
  base-path-prefix: ${BASE_PATH_PREFIX:/users}
  controller-configmap-name: ${CONTROLLER_CONFIGMAP_NAME:qwenpaw-controller-config}
  controller-deployment-name: ${CONTROLLER_DEPLOYMENT_NAME:qwenpaw-controller}
  pod-ready-timeout: ${POD_READY_TIMEOUT:600}
  startup-sync-enabled: ${STARTUP_SYNC_ENABLED:false}
  personal-api-key-jumpcloud-env: ${PERSONAL_API_KEY_JUMPCLOUD_ENV:BASE}
  personal-api-key-timeout-seconds: ${PERSONAL_API_KEY_TIMEOUT_SECONDS:20}
  personal-api-key-file-relative-path: ${PERSONAL_API_KEY_FILE_RELATIVE_PATH:providers/custom/personal-api-key.json}

file:
  root-path: /qwenpaw_nas/personalData
  oss-path: files
  type: ${FILE_TYPE:oss}

---
spring:
  config:
    activate:
      on-profile: sit

  datasource:
    url: ${DB_URL:jdbc:mysql://obproxy-6746.ops.hfr1cloud.bocomm.uatcld:3306/eagpdb?autoReconnection=true&useSSL=true&useUnicode=true&characterEncoding=UTF-8&allowMultiQueries=true&rewriteBatchedStatements=true&serverTimezone=Asia/Shanghai&connectionTimeZone=Asia/Shanghai&forceConnectionTimeZoneToSession=true}
    username: ${DB_USERNAME:eagpuser@teagpua100#obhfcgb21uat}
    password: ${DB_PASSWORD:EAgp@2025}

  data:
    redis:
      host: ${REDIS_HOST:12.244.71.125}
      port: ${REDIS_PORT:6379}
      password: ${REDIS_PASSWORD:EAGP@2026}

wps:
  docs:
    host:
    ak:
    sk:
oss:
  host:
  base-path:

---
spring:
  config:
    activate:
      on-profile: uat

  datasource:
    url: ${DB_URL:jdbc:mysql://12.244.71.125:3306/eagpdb?autoReconnection=true&useSSL=true&useUnicode=true&characterEncoding=UTF-8&allowMultiQueries=true&rewriteBatchedStatements=true&serverTimezone=Asia/Shanghai&connectionTimeZone=Asia/Shanghai&forceConnectionTimeZoneToSession=true}
    username: ${DB_USERNAME:eagpuser@teagpua100#obhfcgb21uat}
    password: ${DB_PASSWORD:EAgp@2025}

  data:
    redis:
      host: ${REDIS_HOST:12.244.71.125}
      port: ${REDIS_PORT:6379}
      password: ${REDIS_PASSWORD:EAGP@2026}

# 肖可
wps:
  docs:
    host: http://bgwd.bocomm.com
    ak: BJIJTOXZJWVBZBLP
    sk: SKdcfgvtrhoffhsd

#  企业级AI服务整合平台—智能体平台
#wps:
#  docs:
#    host: https://cdoc.bankcomm.com
#    ak: DQTCFNJMSBBBZWSV
#    sk: SKlmlxvoxktxjrmj

oss:
  #  host: http://182.119.222.186:8080
  # sit host: http://12.235.230.208:8080
  #  host: http://12.235.197.101:8080
  host: http://12.234.182.67:8080
  base-path: /api/v1/workspace

---
spring:
  config:
    activate:
      on-profile: prd

  datasource:
    # 生产库连接
    url: ${DB_URL:jdbc:mysql://obproxy-1df7.ops.p04r1cloud.bocomm.com:3306/eagpdb?autoReconnection=true&useSSL=true&useUnicode=true&characterEncoding=UTF-8&allowMultiQueries=true&rewriteBatchedStatements=true&serverTimezone=Asia/Shanghai&connectionTimeZone=Asia/Shanghai&forceConnectionTimeZoneToSession=true}
    username: ${DB_USERNAME:eagpuser@teagpprd00#obp2cgb09prd}
    password: ${DB_PASSWORD:EagpUser_123#}

  data:
    redis:
      host: ${REDIS_HOST:12.244.71.125}
      port: ${REDIS_PORT:6379}
      password: ${REDIS_PASSWORD:EAGP@2026}

wps:
  docs:
    host:
    ak:
    sk:
oss:
  host:
  base-path:
